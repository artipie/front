repo:
  type: maven-group
  storage: default
  settings:
    repositories:
      - {{user}}/mprivate1
      - {{user}}/mprivate2
      - {{user}}/mproxy
  permissions:
    {{user}}:
      - upload
      - download
    "*":
      - download
